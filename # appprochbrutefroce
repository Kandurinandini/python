def maxsum(arr,k):
    n=len(arr)
    ms=0
    for i in range(n-k+1):
        s=0
        for j in range(i,i+k):
            s+=arr[j]
        ms=max(ms,s)
    return ms
arr=[1,2,3,4,5,6]
k=3
print(maxsum(arr,k))
